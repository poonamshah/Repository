import java.io.File;
import java.io.IOException;

import com.itextpdf.text.pdf.PdfReader;
import com.itextpdf.text.pdf.parser.PdfTextExtractor;
import com.lowagie.tools.Executable;

public class ReadPDFFile {

	public static void main( String args[] ) {
		/**
		 * @param args
		 *          the command line arguments
		 */
		String INPUTFILE = "C:/Fuelling_Christopher_20101621_Other 1.pdf"; // Specifying the file location.

		try {

			/*
			 * PdfReader reader = new PdfReader( INPUTFILE );
			 * int n = reader.getNumberOfPages();
			 * System.out.println( n );
			 * String str = PdfTextExtractor.getTextFromPage( reader, n ); // Extracting the content from a particular page.
			 * // System.out.println( str );
			 */
			Executable ex = new Executable();
			ex.printDocumentSilent( "C:/Fuelling_Christopher_20101621_Other 1.pdf", true );
			// ex.openDocument("C:/Fuelling_Christopher_20101621_Other 1.pdf");
			//ex.printDocument( "C:/Fuelling_Christopher_20101621_Other 1.pdf" );

		} catch ( Exception e ) {
			System.out.println( e );
		}

	}
}
